import "./Login.css";
import LoginForm from "../../../core/features/auth/components/login-form/LoginForm";

export default function Login() {
    return (
        <main className="login">
            <LoginForm />
        </main>
    );
}
